

## 📋 文档导航

### 核心文档

| 文档 | 内容 | 页数 | 优先级 |
|------|------|------|--------|
| [00_技能图谱总览](./00_技能图谱总览.md) | 本文档 - 全局概览 | - | ⭐ START HERE |
| [06_岗位核心技能深度图谱](./06_岗位核心技能深度图谱.md) | AI模型与部署 | ~25页 | ⭐⭐⭐⭐⭐ |
| [07_系统工具链与底层技能](./07_系统工具链与底层技能.md) | Linux/Docker/C++ | ~23页 | ⭐⭐⭐⭐⭐ |
| [08_应用开发与软技能](./08_应用开发与软技能.md) | FastAPI/飞书/RAG | ~33页 | ⭐⭐⭐⭐⭐ |

### 辅助文档

| 文档 | 内容 | 用途 |
|------|------|------|
| [01_岗位技能培训路径](./01_岗位技能培训路径.md) | 16周学习计划 | 学习规划 |
| [02_顶级论文与研究资源](./02_顶级论文与研究资源.md) | 28篇论文 + 研究室 | 理论深入 |
| [03_Kaggle竞赛与实践项目](./03_Kaggle竞赛与实践项目.md) | 实践项目 | 动手练习 |
| [04_AI_Agent_Sandbox架构方案](./04_AI_Agent_Sandbox架构方案.md) | 创新项目 | 加分项 |
| [05_Demo开发与实验指南](./05_Demo开发与实验指南.md) | 3个完整Demo | 项目实战 |

---

## 🎯 T字型技能体系

```infographic
infographic hierarchy-mindmap-curved-line-compact-card
data
  title AI芯片应用开发全栈技能
  items
    - label 深度技能 (T字竖线)
      children:
        - label 模型部署优化
        - label 后端开发
        - label 芯片SDK集成
    - label 广度技能 (T字横线)
      children:
        - label AI模型理论
        - label 系统工具链
        - label 应用开发
        - label 软技能
```

---

## 一、AI模型与部署 (深度核心)

### 1.1 Transformer & LLM基础

**必须理解的概念** ⭐⭐⭐⭐⭐

```
Self-Attention机制
├── Q, K, V矩阵计算
├── Multi-Head Attention
└── 计算复杂度: O(n²)

Position Encoding
├── 绝对位置编码
└── 相对位置编码 (RoPE)

KV Cache ⭐ 核心优化
├── 为什么需要: 避免重复计算
├── 显存占用: ~0.4GB/1000 tokens
└── 加速比: 理论O(n) vs O(n²)

推理参数
├── Temperature: 控制随机性
├── Top-P/Top-K: 采样策略
└── Repetition Penalty: 防止重复
```

**实践技能**

- [ ] 能够使用HuggingFace Transformers加载模型
- [ ] 理解config.json中的关键参数
- [ ] 会调整推理参数优化输出质量
- [ ] 能够测量TPS、TTFT、吞吐量

**学习资源**: `06_岗位核心技能深度图谱.md` 第1章

---

### 1.2 模型量化技术

**数值精度对比** ⭐⭐⭐⭐⭐

| 精度 | 大小 | 范围 | 适用场景 |
|------|------|------|----------|
| FP32 | 4 bytes | ±3.4×10³⁸ | 训练 |
| FP16 | 2 bytes | ±65,504 | 推理基线 |
| BF16 | 2 bytes | ±3.4×10³⁸ | 训练(TPU) |
| INT8 | 1 byte | -128~127 | 推理优化 |
| INT4 | 0.5 byte | -8~7 | 极致压缩 |

**量化方法**

```
PTQ (训练后量化) ← 重点
├── LLM.int8() (bitsandbytes)
│   ├── 混合精度: 敏感层保持FP16
│   └── 实现: pip install bitsandbytes
├── GPTQ
│   ├── INT4量化
│   └── 需要校准数据集
└── AWQ (MIT Han Lab)
    ├── 激活感知量化
    └── SOTA性能

QAT (量化感知训练)
└── 训练时模拟量化 (不常用)
```

**关键代码**

```python
# bitsandbytes INT8
from transformers import AutoModel, BitsAndBytesConfig

model = AutoModel.from_pretrained(
    "THUDM/chatglm3-6b",
    quantization_config=BitsAndBytesConfig(load_in_8bit=True),
    device_map="auto"
)
# 显存: 13.2GB → 7.1GB (↓46%)
```

**实践技能**

- [ ] 能够使用bitsandbytes进行INT8量化
- [ ] 理解量化的scale和zero-point概念
- [ ] 能够测量量化前后的性能/精度差异
- [ ] 了解GPTQ/AWQ的原理和使用

**学习资源**: `06_岗位核心技能深度图谱.md` 第1.2节

---

### 1.3 ONNX与模型格式

**ONNX价值** ⭐⭐⭐⭐

```
PyTorch模型 → ONNX → 自研芯片推理引擎
             ↓
        TensorRT/OpenVINO/...

优势:
✅ 框架无关
✅ 优化图 (算子融合、常量折叠)
✅ 硬件适配 (芯片厂商实现ONNX Runtime)
```

**实践技能**

- [ ] 能够将PyTorch模型导出为ONNX
- [ ] 会使用Netron查看模型结构
- [ ] 能够使用ONNX Runtime进行推理
- [ ] 理解dynamic_axes的作用

**学习资源**: `06_岗位核心技能深度图谱.md` 第1.3节

---

## 二、系统工具链 (广度基础)

### 2.1 Linux系统管理

**核心命令** ⭐⭐⭐⭐⭐

```bash
# 文件权限
chmod 755 script.sh
chown user:group file.txt

# 进程管理
ps aux | grep python
kill -9 PID
htop  # 交互式监控

# 资源监控
nvidia-smi  # GPU
top / htop  # CPU/内存
df -h  # 磁盘

# 日志分析
tail -f server.log
grep -C 5 "ERROR" server.log
```

**Shell脚本能力**

- [ ] 能够编写自动化部署脚本
- [ ] 理解管道、重定向、后台运行
- [ ] 会使用tmux/screen管理会话
- [ ] 能够排查常见系统问题

**学习资源**: `07_系统工具链与底层技能.md` 第2.1节

---

### 2.2 Docker容器化

**为什么Docker对AI芯片开发极其重要?** ⭐⭐⭐⭐⭐

```
AI芯片环境复杂性:
├── 芯片驱动: 特定版本
├── CUDA/ROCm: 与驱动强绑定
├── 深度学习框架: 依赖特定编译库
└── Python依赖: 数百个包

Docker解决:
✅ 环境一致性
✅ 快速部署
✅ 隔离性
✅ 版本控制
```

**核心技能**

```bash
# 基础操作
docker run -it --gpus all -v $(pwd):/workspace pytorch/pytorch bash
docker ps
docker exec -it container_name bash

# 镜像构建
docker build -t chip-inference:v1.0 .
docker push

# 编排
docker-compose up -d
```

**Dockerfile最佳实践**

```dockerfile
FROM pytorch/pytorch:2.1.0-cuda11.8-cudnn8-runtime

WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY src/ ./src/
EXPOSE 8000

CMD ["uvicorn", "src.server:app", "--host", "0.0.0.0"]
```

**实践技能**

- [ ] 能够编写Dockerfile
- [ ] 理解镜像层缓存机制
- [ ] 会使用docker-compose编排服务
- [ ] 能够进行容器调试

**学习资源**: `07_系统工具链与底层技能.md` 第2.2节

---

### 2.3 C++基础阅读

**为什么需要C++?** ⭐⭐⭐

```
AI芯片SDK:
├── 驱动层: C/C++
├── 算子库: CUDA C++
├── Python绑定: pybind11
└── 构建系统: CMake

需要能力:
✅ 阅读SDK文档和示例
✅ 看懂报错信息
✅ 修改CMakeLists.txt
✅ 编译C++扩展
```

**必备知识**

```cpp
// 指针与智能指针
std::unique_ptr<Model> model;
std::shared_ptr<Tensor> tensor;

// 类与对象
class InferenceEngine {
public:
    InferenceEngine(const std::string& path);
    Tensor forward(const Tensor& input);
private:
    std::shared_ptr<Model> model_;
};

// pybind11绑定
PYBIND11_MODULE(chip_inference, m) {
    py::class_<Model>(m, "Model")
        .def(py::init<const std::string&>())
        .def("inference", &Model::inference);
}
```

**CMake基础**

```cmake
cmake_minimum_required(VERSION 3.18)
project(ChipInference)

find_package(CUDA REQUIRED)
find_package(Python3 REQUIRED)

add_library(chip_inference SHARED src/model.cpp)
target_link_libraries(chip_inference ${CUDA_LIBRARIES})
```

**实践技能**

- [ ] 能够阅读C++ SDK示例代码
- [ ] 理解常见报错信息
- [ ] 会修改CMakeLists.txt配置
- [ ] 能够编译简单的C++扩展

**学习资源**: `07_系统工具链与底层技能.md` 第2.3节

---

## 三、应用开发 (深度核心)

### 3.1 FastAPI后端开发

**核心架构** ⭐⭐⭐⭐⭐

```python
from fastapi import FastAPI
from pydantic import BaseModel

app = FastAPI()

class GenerateRequest(BaseModel):
    prompt: str
    max_tokens: int = 512

@app.post("/generate")
async def generate(request: GenerateRequest):
    # 异步推理
    result = await model_manager.generate(request)
    return result

# 自动生成API文档: /docs
```

**关键特性**

```
✅ 异步支持 (async/await)
✅ 类型提示 + 数据验证 (Pydantic)
✅ 自动API文档 (Swagger UI)
✅ 高性能 (Starlette + uvicorn)
```

**异步编程**

```python
# 并发推理
async def batch_inference(prompts):
    tasks = [generate(p) for p in prompts]
    return await asyncio.gather(*tasks)

# 超时控制
result = await asyncio.wait_for(
    generate(prompt),
    timeout=30.0
)
```

**实践技能**

- [ ] 能够使用FastAPI构建API服务
- [ ] 理解async/await异步编程
- [ ] 会使用Pydantic进行数据验证
- [ ] 能够添加中间件、错误处理

**学习资源**: `08_应用开发与软技能.md` 第3.1节

---

### 3.2 飞书开放平台

**核心流程** ⭐⭐⭐⭐⭐

```
用户消息 → 飞书服务器 → Webhook回调 → 你的服务
                                        ↓
                               AI生成回复
                                        ↓
           飞书API ← 发送消息 ← 你的服务
```

**OAuth 2.0鉴权**

```python
# 获取tenant_access_token
response = requests.post(
    "https://open.feishu.cn/open-apis/auth/v3/tenant_access_token/internal",
    json={"app_id": APP_ID, "app_secret": APP_SECRET}
)
token = response.json()["tenant_access_token"]

# 使用token
headers = {"Authorization": f"Bearer {token}"}
```

**事件处理**

```python
@app.post("/feishu/webhook")
async def feishu_webhook(request: Request):
    body = await request.json()
    
    # URL验证
    if "challenge" in body:
        return {"challenge": body["challenge"]}
    
    # 处理消息
    if event_type == "im.message.receive_v1":
        user_input = parse_message(body)
        ai_response = await generate_response(user_input)
        await send_message(chat_id, ai_response)
    
    return {"code": 0}
```

**交互式卡片**

```python
card = {
    "header": {"title": {"content": "AI助手"}},
    "elements": [
        {"tag": "div", "text": {"content": answer}},
        {
            "tag": "action",
            "actions": [
                {"tag": "button", "text": {"content": "👍"}}
            ]
        }
    ]
}
```

**实践技能**

- [ ] 能够注册飞书开发者账号
- [ ] 理解OAuth 2.0鉴权流程
- [ ] 会处理消息事件和卡片交互
- [ ] 能够调试Webhook回调

**学习资源**: `08_应用开发与软技能.md` 第3.2节

---

### 3.3 RAG检索增强生成

**架构原理** ⭐⭐⭐⭐⭐

```
用户问题
  ↓
Embedding编码
  ↓
向量检索 (FAISS/Milvus)
  ↓
召回Top-K相关文档
  ↓
组合成Prompt
  ↓
LLM生成回答 (带来源)
```

**核心组件**

```python
# 1. Embedding
from sentence_transformers import SentenceTransformer
model = SentenceTransformer('BAAI/bge-small-zh-v1.5')
embedding = model.encode(["AI芯片"])

# 2. 向量存储 (FAISS)
import faiss
index = faiss.IndexFlatIP(512)  # 512维
index.add(embeddings)

# 3. 检索
scores, indices = index.search(query_embedding, k=3)

# 4. 文档切片
from langchain.text_splitter import RecursiveCharacterTextSplitter
splitter = RecursiveCharacterTextSplitter(
    chunk_size=500,
    chunk_overlap=50
)
chunks = splitter.split_text(document)
```

**完整流程**

```python
class RAGSystem:
    def __init__(self, kb_path):
        # 加载知识库
        documents = load_documents(kb_path)
        chunks = split_documents(documents)
        self.vector_store = create_index(chunks)
    
    async def answer(self, question):
        # 检索
        docs = self.vector_store.search(question, k=3)
        
        # 构建prompt
        context = "\n".join([doc.content for doc in docs])
        prompt = f"基于以下信息回答:\n{context}\n\n问题:{question}"
        
        # LLM生成
        answer = await llm.generate(prompt)
        
        return {"answer": answer, "sources": docs}
```

**实践技能**

- [ ] 理解Embedding和向量检索原理
- [ ] 能够使用FAISS构建向量索引
- [ ] 会进行文档切片(chunking)
- [ ] 能够构建完整的RAG系统

**学习资源**: `08_应用开发与软技能.md` 第3.3节

---

## 四、软技能 (必备)

### 4.1 技术文档撰写

**API文档模板** ⭐⭐⭐⭐

```markdown
## 接口名称

**描述**: xxx

**请求**
POST /api/endpoint
Content-Type: application/json

{
  "param1": "value1"
}

**参数说明**
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| param1 | string | 是 | xxx |

**响应示例**
{
  "code": 0,
  "data": {}
}

**错误码**
| 码 | 说明 | 解决 |
|----|------|------|
| 400 | 参数错误 | xxx |
```

**测试报告模板**

```markdown
# 性能测试报告

## 测试环境
- GPU: xxx
- 系统: xxx

## 测试方法
xxx

## 测试结果
| 配置 | 显存 | 速度 |
|------|------|------|
| FP16 | 13GB | 45tok/s |
| INT8 | 7GB | 52tok/s |

## 结论
xxx
```

---

### 4.2 测试方法论

**控制变量法** ⭐⭐⭐⭐

```
测试量化效果时:
✅ 控制: 模型、数据集、硬件
✅ 变量: 仅精度 (FP16 vs INT8)

测试推理加速时:
✅ 控制: 模型、精度、输入
✅ 变量: 推理引擎 (PyTorch vs vLLM)
```

**Edge Case思维**

```python
# 考虑极端情况
- 空输入: prompt = ""
- 超长输入: prompt = "x" * 10000
- 特殊字符: prompt = "🤖💻\n\t"
- 并发: 1000个同时请求
```

---

## 🎯 学习优先级矩阵

```infographic
infographic quadrant-quarter-simple-card
data
  title 技能学习优先级
  items
    - label 第一优先级 (生存技能)
      desc Python + Linux + 模型部署基础
      quadrant 1
    - label 第二优先级 (业务技能)
      desc 飞书开发 + RAG
      quadrant 2
    - label 第三优先级 (进阶技能)
      desc Docker + 量化 + C++
      quadrant 3
    - label 第四优先级 (加分项)
      desc Agent Sandbox + 论文阅读
      quadrant 4
```

---

## 📅 16周学习时间线

```infographic
infographic sequence-timeline-simple
data
  title 完整学习路径
  items
    - label Week 1-4
      desc 基础: Linux/Python/PyTorch/Transformer
    - label Week 5-8
      desc 部署: 量化/ONNX/Docker/性能测试
    - label Week 9-12
      desc 大模型: LLM部署/优化/FastAPI
    - label Week 13-16
      desc 应用: 飞书Bot/RAG/完整项目
```

---

## ✅ 技能自检清单

### AI模型与部署

- [ ] 理解Transformer架构和attention机制
- [ ] 理解KV Cache的作用和实现
- [ ] 能够调整推理参数(temperature, top_p)
- [ ] 掌握bitsandbytes INT8量化
- [ ] 了解GPTQ/AWQ量化方法
- [ ] 能够导出和使用ONNX模型
- [ ] 会测量TPS、TTFT、吞吐量

### 系统工具链

- [ ] 熟练使用Linux命令行
- [ ] 能够编写Shell自动化脚本
- [ ] 掌握Docker基础操作
- [ ] 能够编写Dockerfile
- [ ] 会使用docker-compose编排
- [ ] 能够阅读C++ SDK代码
- [ ] 理解CMake基础

### 应用开发

- [ ] 能够使用FastAPI构建API
- [ ] 理解async/await异步编程
- [ ] 掌握飞书OAuth 2.0鉴权
- [ ] 能够处理飞书消息事件
- [ ] 会构建交互式卡片
- [ ] 理解RAG架构原理
- [ ] 能够使用FAISS进行向量检索
- [ ] 会进行文档切片和Embedding

### 软技能

- [ ] 能够撰写清晰的API文档
- [ ] 会编写测试报告
- [ ] 具备控制变量法思维
- [ ] 考虑Edge Case

---

## 🚀 快速开始指南

### Day 1: 环境准备

```bash
# 1. 安装Conda
wget https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh
bash Miniconda3-latest-Linux-x86_64.sh

# 2. 创建环境
conda create -n chip-dev python=3.10
conda activate chip-dev

# 3. 安装PyTorch
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118

# 4. 安装基础库
pip install transformers accelerate bitsandbytes fastapi uvicorn
```

### Week 1: 第一个模型

```python
# hello_llm.py
from transformers import AutoModel, AutoTokenizer

model_name = "THUDM/chatglm3-6b"
tokenizer = AutoTokenizer.from_pretrained(model_name, trust_remote_code=True)
model = AutoModel.from_pretrained(model_name, trust_remote_code=True).cuda()

response, history = model.chat(tokenizer, "你好", history=[])
print(response)
```

### Week 4: 第一个API

```python
# server.py
from fastapi import FastAPI
from pydantic import BaseModel

app = FastAPI()

class Request(BaseModel):
    prompt: str

@app.post("/generate")
async def generate(req: Request):
    # 调用模型
    response = model.chat(tokenizer, req.prompt)[0]
    return {"text": response}

# 运行: uvicorn server:app --reload
```

---

## 📖 推荐阅读顺序

**新手路径** (第1-4周)
1. `01_岗位技能培训路径.md` - 了解全局
2. `06_岗位核心技能深度图谱.md` 第1.1节 - Transformer基础
3. 动手: 加载第一个模型
4. `07_系统工具链与底层技能.md` 第2.1节 - Linux基础

**进阶路径** (第5-12周)
5. `06_岗位核心技能深度图谱.md` 第1.2节 - 量化技术
6. 动手: INT8量化实验
7. `07_系统工具链与底层技能.md` 第2.2节 - Docker
8. 动手: 容器化部署
9. `08_应用开发与软技能.md` 第3.1节 - FastAPI
10. 动手: 构建推理API

**实战路径** (第13-16周)
11. `08_应用开发与软技能.md` 第3.2节 - 飞书开发
12. `08_应用开发与软技能.md` 第3.3节 - RAG
13. 动手: 飞书AI机器人
14. `05_Demo开发与实验指南.md` - 完整项目
15. `02_顶级论文与研究资源.md` - 理论深化

---

## 💡 学习建议

### 时间分配

```
每天4小时学习:
├── 2小时: 理论学习 (看文档/视频)
├── 1.5小时: 动手实践 (写代码)
└── 0.5小时: 总结笔记
```

### 学习方法

1. **理论与实践结合**: 看一节文档,写一段代码
2. **费曼技巧**: 能用自己的话讲给别人听
3. **刻意练习**: 找不会的反复练
4. **项目驱动**: 以3个Demo为目标
5. **记录成长**: GitHub + 技术博客

---

## 🎓 最终目标

**16周后你将具备**:

✅ **理论知识**
- Transformer架构深度理解
- 量化、推理优化原理
- RAG系统设计

✅ **实践技能**
- 模型部署与优化
- 后端API开发
- 飞书应用集成
- 向量检索系统

✅ **项目作品**
- 大模型部署优化 (性能报告)
- 飞书AI机器人 (可演示)
- Agent Sandbox原型 (创新项目)

✅ **软实力**
- 技术文档撰写
- 问题排查能力
- 系统设计思维

---
